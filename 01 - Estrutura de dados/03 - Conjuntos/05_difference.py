conjunto_a = {1, 2, 3}
conjunto_b = {2, 3, 4}

 # Diferença: elementos de `a` que não estão em `b`
 resultado = conjunto_a.difference(conjunto_b)
print(resultado)

 # Diferença: elementos de `b` que não estão em `a`
 resultado = conjunto_b.difference(conjunto_a)
print(resultado)
